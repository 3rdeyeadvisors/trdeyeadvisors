// PWA components disabled - app runs live without caching interruptions

export const PWAStatus = () => {
  // Component disabled - no more update popups or cache controls
  return null;
};

export const OfflineBanner = () => {
  // Component disabled - the app prioritizes live data
  return null;
};
